# 运行时依赖（仅列代码实际用到的包，避免拖入 anaconda 无关包）
# Python 版本要求：>=3.11（本机开发环境为 3.12.13，已实测通过）
# 建议服务器使用与开发环境一致的 3.12.x，避免版本差异导致的兼容问题
streamlit==1.62.0
langchain-core==1.6.1
langchain-community==0.4.2
ollama==0.6.2
